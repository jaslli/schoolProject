spring:
  # 服务名
  application:
    name: service-oss
  # 环境设置
  profiles:
    active: dev

# 阿里云OSS的设置
aliyun:
  oss:
    file:
      endpoint:
      keyid:
      keysecret:
      bucketname:


# 服务端口
server:
  port: 8082
